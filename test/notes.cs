namespace test
{
    public class Notes
    {
        /*  
            1. add model Item
            2. add DbContext inside data folder as dbcontext
            3. add connection string inside appsettings.json as conn "server,user,password, database"
               note: TrustServerCertificate=true;Trusted_Connection=True must added
            4. add builder inside program.cs to know connection string by sqlServer
            5. add migration by command PM>add-migration with name inittial-mig

            6. PM> update-database




        //cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js
//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css
         */
    }
}
